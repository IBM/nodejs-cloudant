{
  "name": "ibm-clone-app",
  "version": "1.0.0",
  "description": "cloudant + node.js + express",
  "config": {
    "entrypoint": "server/server.js"
  },
  "scripts": {
    "start": "node server/server.js",
    "test": "nyc mocha --exit",
    "devserver": "nodemon server/server.js"
  },
  "devDependencies": {
    "nodemon": "^1.19.1",
    "chai": "^4.2.0",
    "mocha": "^6.0.0",
    "nyc": "^13.3.0"
  },
  "dependencies": {
    "@cloudant/cloudant": "^4.1.1",
    "body-parser": "^1.19.0",
    "cfenv": "^1.2.2",
    "express": "^4.17.1",
    "ibm-cloud-env": "^0.2.6"
  }
}
